<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to "Lista", url_for(:controller => config[:controller], :action => "index") %></li>
      <li><%= link_to "Crear", url_for(:controller => config[:controller], :action => "new") %></li>
      <li class="active"><%= link_to "Editar", url_for(:controller => config[:controller], :action => "edit", :id => @item) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title">Editar <%= config[:name]%></h2>
    <div class="inner">
			<%= config[:content] %>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => config[:sidebar]) -%>